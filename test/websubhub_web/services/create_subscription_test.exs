defmodule CreateSubscriptionTest do
  use ExUnit.Case

  test "creates a subscription" do
    assert true
  end

  test "updates a subscription expired_at for existing topic_url and callback_url" do
    assert true
  end
end
